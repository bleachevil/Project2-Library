<section class="card">
  <a href="/user/{{id}}">
    <header>{{username}}</header>

    <div class="card-body">
      <img src={{thumbnail}} alt="{{description}}">
      <p>By: {{authors}}</p>
    </div>
  </a>
</section>